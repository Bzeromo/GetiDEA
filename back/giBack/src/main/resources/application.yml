spring:
  data:
    redis:
      host: localhost
      port: 6379

    mongodb:
      uri: mongodb://test:1234@localhost:27017

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    jdbc-url: jdbc:mysql://localhost:3306/test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: test
    password: 1234

  thymeleaf:
    cache: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
